package com.tuzicao.emeraldknight.game

enum class ChangeType { ADD, SET, CONDITION }
enum class CheckType { EQUAL, UNEQUAL, MORE, MORE_EQUAL, LESS, LESS_EQUAL, BINARY, NON_BINARY, CONDITION }
enum class CheckOpType { AND, OR }
enum class FuncParas { SCENE, FIGHT, CHOICE }
enum class ActionType { ATTACK, HEAL, CHEAT }
enum class CharacterType { HERO, ENEMY }
enum class HeroId { HAL }
enum class EnemyId { SINESTRO }
